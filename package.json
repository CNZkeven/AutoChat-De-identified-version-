{
  "name": "autochat",
  "private": true,
  "version": "0.1.0",
  "devDependencies": {
    "eslint": "^8.57.0"
  },
  "scripts": {
    "lint": "npm run lint:frontend",
    "test": "npm --prefix frontend-react run test",
    "build": "npm run build:frontend",
    "lint:frontend": "npm --prefix frontend-react ci && npm --prefix frontend-react run lint",
    "build:frontend": "npm --prefix frontend-react ci && npm --prefix frontend-react run build"
  }
}
